<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu"
        android:orderInCategory="1"
        android:title="@null"
        android:icon="@mipmap/ic_news_more"
        app:showAsAction="always">
        <menu>
            <item android:id="@+id/menu_edit_name"
                android:title="修改专辑名"/>
            <item android:id="@+id/menu_delete"
                android:title="删除该专辑"/>
            <item android:id="@+id/menu_edit_article_list"
                android:title="编辑文章列表"/>
            <item android:id="@+id/menu_add_article"
                android:title="添加文章"/>
        </menu>
    </item>

</menu>